build/
.env.local
*.log
.DS_Store
Thumbs.db

# demo package, rebuilt by `make build`
demo.zip

# vendor/ IS committed on purpose - it pins the exact SDK build
# this demo was validated against
!vendor/*.zip